# Drug Design Agent v2 — Requirements
# 核心推理依赖
torch>=2.0.0
transformers>=4.30.0
regex
numpy
pandas
rdkit-pypi  # or rdkit (conda: conda install -c conda-forge rdkit)
scipy

# 对接依赖（可选，运行 Vina Smoke Test 需要）
# vina          # pip install vina  (AutoDock Vina Python bindings)
# meeko         # pip install meeko (PDBQT 配体准备)

# API 依赖（后续 Agent API 开发使用）
fastapi
uvicorn[standard]
pydantic

# 其他
tqdm
scikit-learn
matplotlib
seaborn
